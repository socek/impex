-if name:
  %ul.breadcrumb.col-lg-10.col-lg-offset-1
    -for crumb in crumbs:
      -if loop.last or not crumb.url:
        %li.active -> =crumb.label
      -else:
        %li -> %a href="{{crumb.get_url()}}" -> =crumb.label
