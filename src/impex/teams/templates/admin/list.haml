-extends "impex.application:templates/authencitated.haml"
-block content
    %div.col-lg-12
      %div.bs-component
        %div.jumbotron
          %h1
            Lista drużyn&nbsp;
            %a.btn.btn-small.btn-primary href="{{route_path('teams:admin:create')}}"
              Nowa
          %ul
            -for team in teams:
              %li
                %a href="{{route_path('teams:admin:edit', team_id=team.id)}}"
                  {{team.name}} ({{team.hometown}})

