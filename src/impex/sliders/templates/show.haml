=need('fontawesome')
=need('jquery')
=need('bootstrap')
=need('custom')
=need('bracket')
=need('sliders')
%html
  %head

  %body
    %div.container
      %div.row
        -for tab in tabs.values():
          =tab()
      %script
        ViewConfig = {
          timestamp: {{timestamp}},
          event_id: {{event_id}}
        };
